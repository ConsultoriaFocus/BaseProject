// Descrição mais geral do seu teste ex: CRUD user
describe('Make a Test Example', () => {
    // Descrição mais específica abordagem qual será o enfoque ex: POST user
    describe('Testing boolean type', () => {
        // Descreve qual questão você está verificando ex: Should returns 400 if email is invalid
        test('Should a true variable equals true', () => {
            const exampleVariable = true;
            expect(exampleVariable).toEqual(true);
        });
    });
});
